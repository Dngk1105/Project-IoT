import React from 'react';
import { theme } from '../core/theme';

export const AiStatusPage: React.FC = () => {
  return (
    <div style={{ color: theme.colors.textPrimary }}>
      <h2>Trạng thái AI</h2>
    </div>
  );
};